def binarySearch(nums, target):
    """
    nums: List[int]
    target: int
    """
    left = right = 0
    while ():
        # Prevent overflow
        mid = left + (right - left) / 2
        if (nums[mid] == target):
            pass
        elif nums[mid] < target:
            # left = ...
            pass
        elif nums[mid] > target:
            # right = ...
            pass
    
        return ()